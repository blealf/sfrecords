<div class="container-fluid">
  <div class="col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3">
    <%= bootstrap_form_for(@budget) do |f| %>
      <% if budget.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(budget.errors.count, "error") %> prohibited this budget from being saved:</h2>

          <ul>
          <% budget.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.text_field :title, class: "butn" %>
      </div>

      <div class="field">
        <%= f.text_field :description, class: "butn" %>
      </div>

<!--       <div class="field">
        <%= f.number_field :income, class: "butn" %>
      </div>

      <div class="field">
        <%= f.text_field :spending, class: "butn" %>
      </div>

      <div class="field">
        <%= f.text_field :saving, class: "butn" %>
      </div> -->

      <div class="col-sm-offset-4 col-md-8 actions">
        <%= f.submit class: "btn btn-primary"%>
      </div>
    <% end %>
  </div>
</div>
